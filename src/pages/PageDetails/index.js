import React from "react";

const PageDetails = () => {
    return (
        <section className="section">
            <h4 className="section-title">movie title</h4>
        </section>
    );
};

export default PageDetails;
